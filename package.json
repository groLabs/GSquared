{
  "scripts": {
    "prettier": "prettier --write 'contracts/*.sol' 'test/*.sol' 'contracts/**/*.sol'",
    "prettier-check": "prettier --check 'contracts/*.sol' 'test/*.sol' 'contracts/**/*.sol'"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.5.1",
    "@openzeppelin/contracts": "4.4.0",
    "hardhat": "^2.9.3"
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "prettier-plugin-solidity": "^1.0.0-beta.19"
  }
}
